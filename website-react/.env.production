# 官网生产环境配置
# 站点配置接口地址。默认走同源 /api（由 nginx 反代到 Go server）。
# 若官网与后台不同域，填完整地址，如 https://admin.example.com/api
VITE_API_BASE_URL=/api

# 静态大资源 CDN/OSS 前缀。生产建议配置到 CDN，例如 https://cdn.example.com
# 未配置时本地开发仍使用同源 /assets。
VITE_ASSET_BASE_URL=
